const test=require('node:test');const assert=require('node:assert/strict');const{webcrypto}=require('node:crypto');globalThis.crypto=webcrypto;require('../../js/protected-notes-crypto.js');const security=require('../../js/security-core.js');
test('auto-lock defaults to five minutes and accepts only supported values',()=>{assert.deepEqual(security.normalizeSecuritySettings(null),{protectedNotesAutoLockMinutes:5,lockInBackground:true});assert.equal(security.normalizeSecuritySettings({protectedNotesAutoLockMinutes:15,lockInBackground:false}).protectedNotesAutoLockMinutes,15);assert.equal(security.normalizeSecuritySettings({protectedNotesAutoLockMinutes:999}).protectedNotesAutoLockMinutes,5)});
test('auto-lock state changes at the selected timeout',()=>{const now=1000000,session={unlockedAt:now-60000,lastActivityAt:now-299999};assert.equal(security.shouldAutoLock(session,{protectedNotesAutoLockMinutes:5},now),false);session.lastActivityAt=now-300000;assert.equal(security.shouldAutoLock(session,{protectedNotesAutoLockMinutes:5},now),true)});
