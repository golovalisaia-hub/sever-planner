import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import { fileURLToPath } from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const files=['app.js','notes-pro.js','mobile-ui.js','js/cloud-runtime.js','js/supabase-client.js','js/sync-core.mjs','index.html'];
test('production source has no executable dynamic-code primitives or remote scripts',()=>{const source=files.map(f=>fs.readFileSync(path.join(root,f),'utf8')).join('\n');assert.doesNotMatch(source,/\beval\s*\(|new\s+Function\s*\(|document\.write\s*\(|insertAdjacentHTML\s*\(|<script[^>]+src=["']https?:/i);assert.doesNotMatch(source,/esm\.sh|unpkg\.com|cdn\.jsdelivr\.net/i)});
test('CSP, security headers and local pinned Supabase SDK are present',()=>{const html=fs.readFileSync(path.join(root,'index.html'),'utf8'),headers=fs.readFileSync(path.join(root,'_headers'),'utf8');assert.match(html,/script-src 'self'/);assert.match(headers,/frame-ancestors 'none'/);assert.ok(fs.existsSync(path.join(root,'vendor/supabase.min.js')));assert.match(html,/vendor\/supabase\.min\.js\?v=2\.57\.4/)});
test('user-facing values use safe DOM APIs at critical sinks',()=>{const cloud=fs.readFileSync(path.join(root,'js/cloud-runtime.js'),'utf8'),notes=fs.readFileSync(path.join(root,'notes-pro.js'),'utf8');assert.doesNotMatch(cloud,/migrationCounts'[\s\S]{0,300}\.innerHTML/);assert.match(notes,/querySelector\('h3'\)\.textContent = data\.title/);assert.match(notes,/querySelector\('span'\)\.textContent = item\.text/)});
